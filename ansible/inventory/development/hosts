### Dynamic Environments
[tag_Environment_development]
# alwasy include localhost so we can run playbooks against this inventory and apply the appropriate group_vars
terraformer ansible_host=localhost ansible_connection=local

[development:children]
tag_Environment_development

[development:vars]
infrastructure_env=development
