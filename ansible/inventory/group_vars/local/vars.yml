# inventory/group_vars/local/vars.yml
---
machine_env:
  locale: en_US.UTF-8
  timezone: 'UTC'

default_api:
  DATABASE_HOST: 'localhost'
  INFRASTRUCTURE_ENV: local

default_app:
  API_HOST: 'loclahost:3000'
